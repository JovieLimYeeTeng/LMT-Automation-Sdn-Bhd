# TMS 参考系统事实性 Handoff 文档（零上下文版）

> 用途：供内部成员直接接手，用于后续制作合同、技术选型和 roadmap。  
> 原则：本文**只整理当前已经明确知道的事实**，并把**不确定项单独列出**；**不要求再看原视频或音频**即可理解本文。  
> 重要提醒：本文不是方案书，不做功能承诺，不补充任何未被证据支持的推断。

---

## 1. 文档目的

本文的目标，是把当前已经掌握的上下文整理成一份可直接 handoff 的 Markdown 文档，供团队在**不需要额外观看原始录屏**的前提下，理解：

1. 这个参考系统在做什么。
2. 参考系统中**确实被展示/口述到**的功能有哪些。
3. 哪些点**明确没有被证实**，不能直接写进合同或技术承诺里。
4. 哪些术语存在自动转写误识别风险，外部表述时需要注意边界。

---

## 2. 本文使用的证据来源

### 2.1 已使用来源

1. 当前对话中的业务上下文：有人把一个现有系统的录屏转发过来，希望评估“能不能做”以及“怎么做”。
2. 用户提供的自动转写文本文件：`Screen Recording 2026-01-29 100951.txt`。

### 2.2 未使用或不依赖的来源

1. 不要求团队成员重新观看原始视频/音频。
2. 不依赖任何外部网页、厂商资料、SDK 文档或产品官网。
3. 不依赖任何数据库导出、接口文档、代码仓库或设备说明书。

### 2.3 证据可信度说明

1. 转写文件来自自动转写工具，**存在术语误识别的可能**。
2. 因此，本文对所有可能误识别的术语都单独标明“**转写疑义**”或“**未完全确认**”。
3. 只有当某项内容在转写中被明确提到，或上下文关系极强时，本文才会把它列为“已知事实”。
4. 没有被展示、没有被讲到、或者只能靠猜测补全的内容，一律归入“未确认 / 不在证据范围内”。

### 2.4 本文中的行号说明

下文会使用类似 `T17-T23` 的写法，表示该事实对应转写文件的大致行号范围，便于内部追溯源头。  
这里的 `T` 指代 `Screen Recording 2026-01-29 100951.txt` 这份文本的行号。

---

## 3. 当前已知的业务背景

### 3.1 当前任务背景

从当前对话可知，这不是一个“纯粹学术分析”任务，而是一个**潜在项目交接/立项前整理**任务：

- 外部有人给了一个现有系统的录屏；
- 目标是判断这个系统的边界，并让内部成员据此准备：
  - 合同范围；
  - 技术选型；
  - roadmap。

### 3.2 参考系统的业务性质（基于证据的安全表述）

基于转写内容，这个参考系统可以被**安全地描述为**：

- 一个围绕**员工打卡 / 考勤 / 班次 / 假期 / 报表 / 打卡机数据导入**展开的系统；
- 转写中多次提到 `TMS`、`punch machine / punch kit`、`shift`、`holiday`、`time card`、`OT`、`report` 等关键词；
- 因此，它显然是一个**员工考勤/工时管理方向的系统**，而不是一般性的 CRM、ERP 或纯报表工具。

### 3.3 不能从当前证据中确认的业务命名

以下命名**不能**从现有材料中被完全确认：

1. `TMS` 的全称是什么，没有被明确解释。
2. 参考软件的品牌、厂商、版本号，没有被明确说出。
3. 打卡机/考勤机的品牌、型号、协议、SDK，没有被明确说出。

---

## 4. 参考系统中已经被明确展示/口述到的功能范围

以下内容属于**有证据支持的功能范围**。

---

### 4.1 登录与首次激活

已知事实：

1. 软件有登录入口，需要输入 `username` 和 `password`。`T3-T5`
2. 首次登录/首次使用时，不只是正常登录，还需要经过一个注册/激活步骤。`T7-T13`
3. 激活过程里需要输入 `license key`，输入后才能 activate。`T9-T13`

可以安全表述为：

- 参考系统存在**账号登录**机制；
- 参考系统存在**License 激活**机制；
- 至少在首次使用阶段，激活是一个必要流程。

不能确认的点：

- 无法确认 license 是按机器、按公司、按用户还是按部署实例授权；
- 无法确认是否支持在线激活 / 离线激活；
- 无法确认密码策略、角色权限策略、审计日志是否存在。

---

### 4.2 员工主数据（Employee Profile / File Maintenance）

已知事实：

1. 系统有 `File maintenance` 模块。`T15-T17`
2. 在初始设置中，需要录入员工信息（profile）。`T17-T19`
3. 员工资料新增时，需要输入 `enroll number`。`T19-T23`
4. 讲解者明确强调：`enroll number` 非常重要，它要与打卡机上的编号一致，系统才能匹配到该员工的详细资料。`T21-T23`
5. 员工资料中至少还包含姓名等基础信息。`T25`
6. 员工资料里涉及 `rest day`（休息日）设置。`T25-T30`
7. 讲解者举例说明：不同客户场景不同，例如餐饮类员工可能周日也上班，因此休息日可以不是固定周日，可以按实际情况选择。`T27-T30`

可以安全表述为：

- 系统至少支持**员工档案维护**；
- 员工与打卡机之间存在一个关键映射字段：`enroll number`；
- 这个字段和设备端编号的一致性，对匹配打卡数据是必要的；
- 员工档案中包含休息日概念。

不能确认的点：

- 无法确认员工档案字段全集；
- 无法确认是否包含工号、身份证、手机号、部门编号、职位、薪资等字段；
- 无法确认 `enroll number` 是否可修改、是否唯一、是否允许一个员工绑定多设备编号。

---

### 4.3 班次（Shift）设置

已知事实：

1. 系统有 `shift` 设置。`T33-T35`
2. `shift` 用来定义员工何时开始工作、何时结束工作。`T35`
3. `shift` 中包含 `start` 和 `end`。`T37`
4. `shift` 中涉及 `lunch hour`。`T37-T39`
5. 讲解者说明：有些客户场景没有固定午休，因此 lunch hour 在某些场景下不重要。`T37-T40`
6. 讲解者还说明：系统支持一种比较灵活的设置方式，例如不严格限制几点去吃饭，只要求 1 小时后回来。`T41-T43`
7. 系统还支持按总工时来算，例如“几点来上班都可以，但只要做满 8 小时”。`T45-T47`
8. 系统可配置多个 shift，至少口述提到了 2、3 个 shift 的情况。`T49`

可以安全表述为：

- 参考系统支持**班次主数据维护**；
- 班次里至少包括上下班时间；
- 班次规则里至少涉及午休和弹性工时；
- 系统支持多个班次。

不能确认的点：

- 无法确认弹性规则的精确计算方式；
- 无法确认是否支持跨天班、夜班、轮班周期、宽限时间、自动扣休等复杂规则；
- 无法确认班次与员工/部门/公司的绑定方式。

---

### 4.4 年度假期 / Holiday 设置

已知事实：

1. 系统有 `holiday` 相关设置。`T53-T57`
2. 讲解者说明：一年做一次设置即可。`T53`
3. 设置后，这些 holiday 会自动体现在 report 中。`T53-T57`
4. 不需要针对每个员工逐个去单独设置 holiday。`T55-T57`

可以安全表述为：

- 系统支持**年度 holiday 主数据**；
- holiday 会自动进入报表逻辑；
- holiday 并不是每个员工手工单独维护一遍。

不能确认的点：

- 无法确认 holiday 是按公司、按地区、按国家还是全局配置；
- 无法确认是否支持半天假期、调休、补班等复杂日历规则。

---

### 4.5 Preference / 部门 / Leave Time

已知事实：

1. 讲解者提到系统里有一类 `preference` 设置。`T57-T63`
2. 其中明确提到了 `department` 和 `leave time`。`T59-T61`
3. 讲解者明确说：`department`、`leave time` 是重要的，需要有。`T59-T63`
4. 同一段里提到了 `part time leave`，但口述上表达的是“这个可能不需要也可以”。`T61`

可以安全表述为：

- 系统中至少存在与 `department`、`leave time` 相关的基础配置；
- 在讲解者视角里，`department`、`leave time` 是核心项。

不能确认的点：

- 无法确认 `leave time` 的精确定义，是假别、请假时段、请假单位，还是其他设置；
- 无法确认 part-time leave 的具体业务规则。

---

### 4.6 手动换班（Change Shift）

已知事实：

1. 系统的 `posting` 相关操作中，存在 `change shift`。`T63-T69`
2. 讲解者举例：某员工原本属于 `shift 1`，但在某天（示例中提到 2026 年 1 月某一天）可以改成 `shift 2`。`T65-T69`
3. 这种 change shift 是可以手动更新的。`T67-T69`
4. 更新后，系统记录会知道该员工哪一天是 `shift 1`、哪一天是 `shift 2`。`T67-T69`

可以安全表述为：

- 系统支持**按具体日期对员工进行手动换班**；
- 换班是逐日生效的，而不是只能修改全局默认班次。

不能确认的点：

- 无法确认 change shift 是否支持批量操作；
- 无法确认是否支持审批、导入、模板套用、冲突检测等功能。

---

### 4.7 Auto Shift（自动班次判断）

已知事实：

1. 在员工 profile 附近，讲解者提到 `auto shift`。`T71-T73`
2. `auto shift` 可以关联多个 shift，例如口述里提到 1、2、3、4 个 shift。`T73`
3. 讲解者举例：如果员工早上打卡，系统自动判到 `shift 1`；如果下午打卡，自动判到 `shift 2`。`T75-T77`
4. 讲解者明确说，这样就不用手动设置。`T75`

可以安全表述为：

- 系统支持某种**自动判班**能力；
- 自动判班的依据至少与员工打卡时间有关；
- 自动判班的目标是减少手动设置。

不能确认的点：

- 无法确认 auto shift 的完整判定规则；
- 无法确认是否支持跨天班、班次优先级、冲突处理、自动回填等。

---

### 4.8 Time Card（打卡记录查看与人工修正）

已知事实：

1. 系统有 `time card` 功能。`T77-T79`
2. time card 页面会显示时间记录。`T77-T79`
3. 该页面可以按员工（通过 enroll number / 人员）去查找并编辑。`T81-T85`
4. 讲解者说明：如果有人忘记 punch card，HR 可以帮忙补回去/改回去。`T89-T91`
5. 转写中提到某些代码，例如 `CL`、`KO`，并口头解释成 `check in`、`check out`。`T87`

可以安全表述为：

- 系统支持查看和编辑员工打卡时间卡；
- 系统支持 HR 对漏打卡等情况进行人工处理；
- 系统中存在某些打卡状态/事件代码。

**转写疑义 / 边界说明：**

- `CL`、`KO` 的字母是否完全准确，只能说是转写结果；
- 虽然讲解者口头解释成 `check in / check out`，但由于未直接核对 UI 标签，合同或设计文档里**不应把这两个代码字面值当成已完全确认的正式编码**。

---

### 4.9 一项与 Leave 相关的申请功能（转写疑义）

已知事实：

1. 在 time card 后，转写文件出现了 `Lift application` / `lift tab` 这样的字样。`T91-T95`
2. 同一系统前文明确提到过 `leave time`。`T59-T61`
3. 讲解者说明：在这里进行申请，之后报表里会体现出来。`T93-T95`

**安全表述方式：**

- 系统里存在一项**与请假/假别高度相关的申请功能**；
- 该功能会影响后续 report 输出。

**边界说明：**

- 自动转写把该功能记成了 `Lift application`，但这很可能是术语误识别；
- 在没有 UI 原文或正式字段定义前，**不能把这个模块的正式英文名称写死**。

---

### 4.10 OT 设置

已知事实：

1. 转写里明确提到 `OT`。`T97-T99`
2. 同时也提到了 `OT setting`。`T97-T99`

可以安全表述为：

- 系统中有**加班（OT）相关设置或处理功能**。

不能确认的点：

- 无法确认 OT 的申请流、审批流、计算规则、费率规则或与班次/休息日的交互规则。

---

### 4.11 Employee Rest Day Maintenance / 自定义休息日维护

已知事实：

1. 转写中明确提到 `Employee rest day maintenance`。`T99`
2. 讲解者说明：员工的休息日不一定每周固定相同。`T101-T107`
3. 讲解者举例说明：这一周可能这样，下周可能是 Monday，或者是其他自定义情况。`T101-T107`
4. 因而系统存在 `custom` 的休息日维护。`T107`

可以安全表述为：

- 系统支持**员工层面的休息日维护**；
- 休息日可以不是固定周规则，而可以是更灵活/自定义的。

不能确认的点：

- 无法确认自定义休息日是按日期维护、按周模式维护，还是按排班逻辑自动生成。

---

### 4.12 报表（Reports）

已知事实：

1. 系统有专门的 `report` 区域，而且“有很多”。`T109-T111`
2. 报表里明确提到了：
   - `employee master list`；`T115-T121`
   - `shift master list`；`T115-T123`
   - 一类详细的出勤报表；`T125-T153`
   - `summary`。`T155-T165`

#### 4.12.1 Employee Master List

已知事实：

1. 系统能输出 `employee master list`。`T115-T121`
2. 该报表会列出员工相关主数据。`T117-T121`

#### 4.12.2 Shift Master List

已知事实：

1. 系统能输出 `shift master list`。`T115-T123`
2. 讲解者举例：其中会列出 `shift 1`、`shift 2`，若有 3、4、5、6 也会列出来。`T123`

#### 4.12.3 详细考勤报表

已知事实：

1. 讲解者提到一类“常用”的详细报表。`T125-T129`
2. 该报表支持选择日期范围。`T135-T139`
3. 讲解者明确说，HR 会需要查询两三个月前的记录，因此日期范围选择很重要。`T135-T139`
4. 报表内容中明确提到了：
   - `check in`；`T141`
   - `check out for break`；`T141`
   - `length of work`；`T133`
   - `late time`；`T145`
   - 一项与“提早离开/早退”相关的项目，转写成 `UT`。`T147`
   - 工作天数、工作小时数、休假/缺勤等总体信息。`T151-T153`
5. 讲解者强调：HR 需要的关键信息都应该在这里。`T149-T153`

**转写疑义 / 边界说明：**

- 转写里把这类报表名写成了 `daily` 或 `Monday report` 一类说法，但这很可能包含转写误差；
- **可以确定的是：存在一类详细考勤报表**，但**不能仅凭转写文件确认其正式菜单名**。
- `UT` 的正式含义也不能仅凭转写完全写死；可安全理解为一项与“早退/提前离开”相关的指标，但正式编码仍待确认。

#### 4.12.4 Summary 报表

已知事实：

1. 系统有 `summary`。`T155-T165`
2. 讲解者说这个 summary 很重要，老板可以直接看。`T157-T163`
3. summary 能快速看出：
   - 某员工工作了多少天；`T159-T163`
   - absent 多少天。`T163`

可以安全表述为：

- 系统支持面向管理者快速浏览的汇总报表。

#### 4.12.5 报表筛选与范围限制

已知事实：

1. 报表支持按 `company` 选择。`T167`
2. 讲解者说可以设置 `few company`，说明系统至少考虑了多公司/多主体视角。`T167`
3. 报表支持按 `department` 过滤。`T167`
4. 报表也支持只查某个人。`T169`
5. 报表打印存在一个限制：一次只能打印一个月。`T171-T177`
6. 讲解者举例：不能跨成“15 号到下个月 15 号”这种范围。`T173-T177`

可以安全表述为：

- 参考系统的报表至少支持 company / department / employee 维度过滤；
- 参考系统存在“**单次打印只能按单月**”的限制。

不能确认的点：

- 无法确认报表是直接打印、导出 PDF、导出 Excel，还是都支持；
- 无法确认多公司是多租户、同库多公司，还是只是报表筛选维度。

---

### 4.13 Tools / Company / Import

已知事实：

1. 在 `Tool` 区域，讲解者提到可以“加 company name”。`T179-T181`
2. 同一处还提到 `import`。`T179-T181`

可以安全表述为：

- 系统某个工具区至少与 `company` 和 `import` 有关。

边界说明：

- 仅能确认讲解中提到了这些词；
- 不能确认是否是新增公司、导入员工、导入打卡数据、导入假期、还是其他导入功能。

---

### 4.14 设备通信 / Download / Raw Data 获取

这是当前证据里非常重要的一部分。

已知事实：

1. 系统里有 `Download` 相关功能。`T191`
2. 下载/通信前，需要先选择哪一台 machine。`T201`
3. 设备连接方式里，明确提到了 `network` 或 `wifi`。`T203`
4. 对于 network/wifi 连接，需要先设置 `IP address`。`T203-T205`
5. 然后点击 `Open COM` 去 connect。`T205-T208`
6. 连接后，进入 `Log data management`。`T209-T213`
7. 如果当前没有连到设备，会显示 `no device`。`T211-T215`
8. 除 network/wifi 外，还明确提到了 `USB` 方式。`T215-T219`
9. 转写中还提到去找某个 file。`T221`
10. 系统会读取 `DAT file`。`T223-T229`
11. 讲解者明确说：`DAT file` 带有 security，只有这个软件能读。`T225-T229`
12. 相对地，`text file` 是谁都可以打开的。`T229-T231`
13. 讲解者明确澄清：这个步骤不是去“拿 report”，而是与 machine 通信、把 machine 的 raw data 取回来。`T231-T237`
14. raw data 取回来之后，再去报表模块看自己要的 report。`T235-T237`

可以安全表述为：

- 参考系统支持**从打卡设备获取原始打卡数据**；
- 数据获取方式至少包括：
  - network / wifi + IP address；
  - USB；
  - 文件读取。
- 获取到的是 `raw data`，而不是最终报表；
- 系统随后再基于 raw data 生成报表；
- 至少存在两类文件概念：`DAT file` 与 `text file`；
- `DAT file` 在讲解者描述中属于该软件专有/受限可读格式。

**转写疑义 / 边界说明：**

- `Open COM` 是否真的是传统串口 COM 概念，还是只是 UI 按钮名字，无法从当前证据完全确认；
- `DAT file` 的结构、加密方式、是否厂商专有、是否可逆解析，均未被证实；
- `ATL` / `AGL` 之类字样在转写里也出现了，但准确术语无法确认，不宜写死。

---

## 5. 从证据中可以还原出的参考系统工作流

以下工作流不是凭空推测，而是把转写中前后关系串起来后的**最小闭环还原**：

1. 用户打开 TMS 软件并登录。`T3-T5`
2. 首次使用时，需要 license key 激活。`T7-T13`
3. 在 `File maintenance` 中录入员工资料。`T15-T25`
4. 员工资料里，`enroll number` 必须和打卡机编号匹配。`T21-T23`
5. 设置班次、holiday、department / leave time 等基础资料。`T33-T63`
6. 在日常运营里，可做手动换班、自动判班、time card 修正、OT、rest day 维护。`T63-T107`
7. 通过 machine / network / wifi / USB / file 读取 raw data。`T191-T237`
8. 取回 raw data 后，进入 report 查看各种明细报表和 summary。`T109-T177`、`T235-T237`

可以安全总结为：

- 这是一个“**主数据维护 → 原始打卡数据采集 → 考勤结果/报表输出**”的系统闭环。

不能确认的点：

- 中间是否有独立的计算引擎、定时任务、审批流、复核流、薪资联动，没有证据。

---

## 6. 可以直接作为合同基线的“已证实能力清单”

> 下面这一节不是新增需求，只是把**已经被证实存在**的能力抽成清单。  
> 若后续合同要写“参照现有系统达到等效能力”，这一节可作为起点；但所有未确认项仍需单独澄清。

### 6.1 已证实的模块级能力

1. 账号登录。
2. 首次 License 激活。
3. 员工档案维护。
4. 关键映射字段：`enroll number`。
5. 休息日设置。
6. 班次设置。
7. 午休/弹性工时相关班次规则。
8. 多班次。
9. 年度 holiday 设置。
10. department / leave time 相关设置。
11. 手动 change shift。
12. auto shift。
13. time card 查看与人工编辑。
14. 一项与请假/假别相关的申请功能（正式名称未确认）。
15. OT 设置。
16. employee rest day maintenance / 自定义休息日维护。
17. employee master list。
18. shift master list。
19. 一类详细考勤报表。
20. summary 报表。
21. 按 company / department / individual employee 过滤报表。
22. 从设备 / 文件获取 raw data。
23. network / wifi / USB / file 这几类取数方式至少被提到。
24. DAT file 与 text file 两类数据文件概念。

### 6.2 已证实的约束/行为

1. 员工的 `enroll number` 必须与设备编号匹配，系统才能匹配员工详情。
2. holiday 设置后会自动出现在报表中。
3. change shift 是按具体日期生效。
4. auto shift 至少可以根据打卡时间自动落到不同班次。
5. time card 可由 HR 对漏打卡情况进行人工处理。
6. 报表支持 company / department / individual employee 筛选。
7. 单次打印限制为单月范围，不能跨成 15 到次月 15 这样的周期。
8. Download 模块的目标是从 machine 取 raw data，而不是直接拿最终报表。
9. `DAT file` 在讲解者口径中是受限可读格式，`text file` 则是可普遍读取格式。

---

## 7. 当前证据明确不足以支持的内容

以下内容**不要因为行业常识而自动补进合同或技术承诺**；它们在当前证据里没有被充分证实：

### 7.1 硬件/设备层

1. 打卡机品牌、型号。
2. 是否指纹、刷卡、人脸、掌静脉或其他识别方式。
3. 是否有官方 SDK / API / 协议文档。
4. 是否支持实时推送，还是只能轮询/导出。
5. 设备端时钟同步方式。
6. 设备端权限、管理员账号、固件升级方式。

### 7.2 软件架构层

1. 这是桌面系统、Web 系统还是混合架构，没有被正式说明。
2. 数据库类型、表结构、主键设计、部署模式，没有被说明。
3. 是否支持云端、多租户、分店/门店、跨地域部署，没有被说明。
4. 是否存在独立服务端、agent、同步服务、任务调度器，没有被说明。

### 7.3 业务规则层

1. late、early out、OT 的详细计算公式。
2. 跨天班、夜班、轮班、宽限时间、自动扣休、加班门槛等复杂规则。
3. 请假审批流、OT 审批流。
4. 与薪资、Payroll、HRMS 的集成关系。
5. 异常打卡、重复打卡、跨设备打卡冲突处理。
6. 审计日志、修改痕迹、回滚机制。

### 7.4 安全与权限层

1. 用户角色。
2. 访问控制。
3. 操作日志。
4. 数据加密。
5. 密码策略。
6. License 的授权边界。

### 7.5 输出能力层

1. 是否支持 Excel/PDF 导出。
2. 是否支持自定义报表模板。
3. 是否支持邮件发送、自动任务、排程导出。

### 7.6 项目属性层

当前证据**没有显示**以下内容：

1. 没有显示固件开发。
2. 没有显示 PCB、驱动、传感器开发。
3. 没有显示底层嵌入式调试界面。

因此，**仅凭当前证据**，不能把这个项目定义成“必然包含嵌入式开发”的项目。  
当前证据能证明的是：**该参考系统需要与现有打卡设备/文件进行集成**。

---

## 8. 转写疑义清单（外部表述时必须谨慎）

这一节非常关键。以下词在内部讨论时可以参考，但**在合同、正式 PRD、技术方案中不宜直接写死字面值**，除非后续再获得 UI 截图、正式菜单文字或设备文档确认。

| 转写结果 | 当前可安全理解的含义 | 风险说明 |
|---|---|---|
| `TMS` | 一个考勤/工时/打卡管理方向的软件系统 | 全称未知 |
| `punch kit` / `machine` | 打卡机/考勤机 | 品牌型号未知 |
| `Lift application` / `lift tab` | 高度疑似与 leave / 请假相关的功能 | 术语很可能被误转写 |
| `daily Monday report` / `Monday report` | 一类详细考勤报表 | 正式菜单名不清楚，可能是 monthly / daily / month card 等近音误转 |
| `CL` / `KO` / `UT` | 某些打卡/状态代码，其中至少有 check in / check out / early leave 相关解释 | 代码字面值和正式含义都未完全核对 |
| `Open COM` | 某个用于连接设备的按钮/动作 | 未知是否真指串口 COM，还是按钮名称 |
| `ATL` / `AGL` | 某种文件/格式相关名词 | 转写不稳定，不能写死 |

---

## 9. 合同与技术选型前，必须单独澄清的开放项

> 这一节不是在补需求，而是在明确：**哪些内容当前没有证据，必须在合同前单独确认**。

### 9.1 设备侧必须澄清

1. 设备品牌和型号是什么？
2. 设备当前通过什么方式导出数据：network、wifi、USB、DAT、text，还是多种都在用？
3. 是否存在官方 SDK / API / 协议说明书？
4. 当前项目目标是“兼容现有设备”，还是“替换设备”，还是“先导入文件后再做直连”？

### 9.2 规则侧必须澄清

1. 班次规则是否只需支持录屏中的基础能力，还是还要包含夜班、跨天班、轮班等复杂规则？
2. auto shift 的判定规则是否只有简单时间窗口，还是更复杂？
3. OT、late、early out、rest day、holiday 的正式计算口径是什么？
4. time card 的人工修正规则、权限和留痕要求是什么？

### 9.3 报表侧必须澄清

1. 需要完全复刻旧报表版式，还是只需字段等效？
2. 需要导出什么格式？
3. 是否必须保留“单月打印”的旧限制，还是允许升级为任意区间？
4. 多公司、多部门是报表过滤需求，还是权限隔离需求？

### 9.4 系统形态必须澄清

1. 目标系统是桌面版、Web 版，还是 Web + 本地采集组件？
2. 部署在单机、局域网还是云端？
3. 是否需要移动端？
4. 是否需要审批流、日志、权限、通知、Payroll 集成？

---

## 10. 一个可直接复制给团队的“事实性摘要”

下面这段可直接作为 handoff 摘要使用：

> 当前参考系统是一个员工考勤/工时管理方向的软件。已证实功能包括：账号登录、首次 license 激活、员工档案维护、以 enroll number 与打卡机编号做匹配、班次设置、holiday 设置、department / leave time 相关设置、手动 change shift、auto shift、time card 查看与人工修正、OT、employee rest day maintenance、多类报表（包括 employee master list、shift master list、详细考勤报表、summary），以及从 machine / network / wifi / USB / file 读取 raw data，再基于 raw data 生成报表。已证实的限制包括：报表支持 company / department / employee 过滤，但单次打印只能按单月。未证实的内容包括：设备品牌型号、SDK/API、详细计算规则、权限、审批、导出格式、部署架构、是否需要嵌入式开发等。转写中若干术语存在误识别风险，特别是 leave/lift、monthly/Monday、CL/KO/UT、Open COM 等字样，外部文档不要直接写死，需后续再确认。

---

## 11. 证据索引（供内部追溯）

### 11.1 登录与激活

- 登录：`T3-T5`
- 首次注册 / license key / activate：`T7-T13`

### 11.2 员工档案与 enroll number

- File maintenance / profile：`T15-T19`
- enroll number 与 machine 编号匹配：`T19-T23`
- 姓名 / rest day：`T25-T30`

### 11.3 班次与 holiday

- shift / start / end / lunch / flexible / 8 hours：`T33-T49`
- holiday 一年设置一次并自动进报表：`T53-T57`
- department / leave time：`T59-T63`

### 11.4 运营动作

- change shift：`T63-T69`
- auto shift：`T71-T77`
- time card edit / 漏打卡 HR 处理：`T77-T91`
- leave-related application（转写疑义）：`T91-T95`
- OT：`T97-T99`
- employee rest day maintenance：`T99-T107`

### 11.5 报表

- report 区域与报表类别：`T109-T123`
- 详细考勤报表字段与日期筛选：`T125-T153`
- summary：`T155-T165`
- company / department / employee 过滤：`T167-T169`
- 单月打印限制：`T171-T177`

### 11.6 工具与设备取数

- Tool / company / import：`T179-T181`
- Download / machine / network / wifi / IP / Open COM：`T191-T208`
- Log data management / no device / USB：`T209-T219`
- DAT file / text file / raw data：`T221-T237`

---

## 12. 最终边界声明

1. 本文**不**承诺可以完全复刻参考系统，只说明参考系统里**已经被证实存在的内容**。
2. 本文**不**把任何行业常见功能自动补进范围，例如审批、权限、审计、Payroll、移动端、云部署等。
3. 本文**不**把任何术语误转写直接当成正式字段名或正式菜单名。
4. 本文**不**把该项目自动定性为嵌入式项目；当前证据只能证明存在**与打卡设备/文件的集成需求**。
5. 若后续合同、技术选型、roadmap 要对外输出，必须先基于本文第 9 节的开放项做进一步确认。

---

## 13. 一句话结论

**仅基于当前证据，可以明确这是一套“员工考勤/工时管理 + 打卡设备/文件取数 + 报表输出”的参考系统；可以明确其功能边界的大框架，但还不能据此直接承诺设备兼容细节、规则计算细节、架构形态或嵌入式开发范围。**
